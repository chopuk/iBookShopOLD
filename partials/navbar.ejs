<section class="mb-4">
  <nav class="navbar navbar-expand-lg bg-dark navbar-dark py-2 fixed-top">
    <div class="container">
      <a href="/book/booklist" class="navbar-brand">iBookShop</a>

      <button
        class="navbar-toggler"
        type="button"
        data-bs-toggle="collapse"
        data-bs-target="#navmenu"
      >
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navmenu">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item">
            <a href="/about" class="nav-link">About Us</a>
          </li>

          <% if(loggedIn) { %>

            <li class="nav-item">
              <a href="#" class="nav-link welcome">Welcome, <%= user.username %>!</a>
            </li>
            <li class="nav-item">
              <a href="/user/profile" class="nav-link">My Profile</a>
            </li>
            <li class="nav-item">
              <% if(typeof itemsCount != 'undefined') { %>
                <% if(itemsCount > 0) { %>
                  <a href="/cart/list" class="nav-link itemsInCart">Cart (<span><%= itemsCount %></span>)</a>
                <% } else { %>
                  <a href="#" class="nav-link">Cart (<span><%= itemsCount %></span>)</a>
                <% } %>
              <% } else { %>
                <a href="#" class="nav-link">Cart (0)</a>
              <% } %>
            </li>
            <li class="nav-item">
              <a href="/order/myorders" class="nav-link">My Orders</a>
            </li>
            <li class="nav-item">
              <a href="/user/logout" class="nav-link">Logout</a>
            </li>

          <% } else { %>

            <li class="nav-item">
              <a href="/user/register" class="nav-link">Register</a>
            </li>
            <li class="nav-item">
              <a href="/user/login" class="nav-link">Login</a>
            </li>

          <% } %>

        </ul>
      </div>
    </div>
  </nav>
</section>
